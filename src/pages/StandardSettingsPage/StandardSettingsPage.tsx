import React from 'react';

import SettingsForm from '../../components/SettingsForm';

export const StandardSettingsPage = (props: any) => {
  return (
    <SettingsForm  />
  );
};

export default StandardSettingsPage;