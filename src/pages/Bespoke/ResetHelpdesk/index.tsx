import React, { FC } from 'react';

interface IProps {
  path: string;
}

const ResetHelpdeskPage: FC<IProps> = () => {

  return (
    <div>
      <p>Reset Helpdesk page</p>
    </div>
  );
};

export default ResetHelpdeskPage;
