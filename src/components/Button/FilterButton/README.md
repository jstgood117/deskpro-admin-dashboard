# FilterButton Component Contract

- it has Props of:
  children?: ReactNode - element inside button
  onClick?: (e: any) => void - Button Click event
- it has no state
- it renders a `<button>` element, `<svg>` element, `<div>` element
